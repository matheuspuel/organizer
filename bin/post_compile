#!/usr/bin/env bash

cd "$1" || exit 1
echo "-----> Compressing (Compiling SCSS)"
python manage.py compress --force --settings=organizer.settings.production
echo "-----> Collecting static files"
python manage.py collectstatic --noinput --traceback --settings=organizer.settings.production
